# 复制为 .env 并填入真实值（.env 不会进 git）
# 后台管理密钥：访问 /admin 后台、创建朋友令牌时使用
ADMIN_KEY=admin-change-me

# 你自己常用的网关 key（owner，免配额）；客户端用 Authorization: Bearer <这个值>
GATEWAY_KEY=sk-local-change-me

# 各上游的 API key（按需填，留空的渠道会自动禁用）
DEEPSEEK_API_KEY=
QWEN_API_KEY=
OPENROUTER_API_KEY=
OPENAI_API_KEY=
