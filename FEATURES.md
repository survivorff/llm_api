# 功能清单与市场对比

## 一、现有功能（已实现 + 已测试）

### 网关核心
- OpenAI 兼容 `POST /v1/chat/completions`（流式 + 非流式）
- `GET /v1/models` 列出可用模型
- 多渠道按模型名路由：精确 → 前缀(`claude-*`) → 通配(`*`)兜底
- 同渠道多 key 轮询 + 失败自动重试（429/5xx 换 key、换渠道）
- `model_map` 别名：短名（`claude`）映射到上游真实 slug

### 多用户与管控（给朋友用）
- 令牌管理：后台创建/启停/删除独立令牌，每人一个 `sk-...`
- **配额**：每令牌可设 token 上限，超额自动 429
- **有效期**：可设过期时间 / 有效天数，过期自动失效
- **限速**：每令牌 RPM（每分钟请求数）限制
- **模型白名单**：限制某令牌只能用指定模型
- **自助查询** `GET /v1/me`：朋友用自己的 key 查用量/剩余/有效期
- 角色分离：admin（`ADMIN_KEY`）/ owner（`GATEWAY_KEY`，免配额）/ 朋友令牌

### 后台与计量
- `/admin` 网页面板：建号、启停、看用量
- 用量按"人 / 渠道"统计；流式请求也能正确计入 token
- 全部用量写 SQLite

### 工程
- 配置驱动（YAML + `${ENV}`），空 key 渠道自动禁用
- Docker / docker-compose / run.sh 一键启动
- 21 个自动化测试（端到端，无需真实 key）

## 二、与市场中转站（new-api / one-api）对比

| 能力 | 本项目 | new-api 等 | 说明 |
|---|---|---|---|
| OpenAI 兼容接口 | ✅ | ✅ | |
| 多渠道/多 key/故障转移 | ✅ | ✅ | |
| 令牌管理 + 配额 | ✅ | ✅ | |
| 有效期 / 限速 / 模型白名单 | ✅ | ✅ | |
| 自助用量查询 | ✅ | ✅ | |
| 后台面板 | ✅ 极简 | ✅ 功能丰富 | 我们够朋友用 |
| 按"钱/余额"计费 + 模型倍率定价 | ❌ | ✅ | 朋友互助不需要，按 token 配额即可 |
| 兑换码充值 | ❌ | ✅ | 转售才需要，刻意不做 |
| 用户分组 / 多渠道分组策略 | ❌ | ✅ | 规模化才需要 |
| embeddings / images 等多端点 | ❌ | ✅ | 编码场景主要用 chat |
| 渠道健康巡检/自动禁用 | ❌(仅请求级重试) | ✅ | 后续可加 |
| 加密货币充值结算 | ❌(路线图) | 多数❌ | **这是我们将来的差异化点** |

> 结论：作为"自用 + 朋友互助"的网关，核心功能已经齐了。
> 刻意不做"兑换码 / 余额计费 / 公开售卖"那一套——那是转售生意，涉及合规，且不是当前目标。

## 三、路线图（按需再加）
- [ ] 渠道健康巡检 + 连续失败自动熔断
- [ ] 加密货币充值与按量结算（合法差异化核心）
- [ ] Anthropic 原生 `/v1/messages` 协议
- [ ] embeddings 端点透传
- [ ] 成本估算（按模型价格表换算 token→¥）
